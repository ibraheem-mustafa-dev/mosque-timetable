# Archived Handoffs — Mosque Timetable

Session handoffs are archived here using the naming convention:

    YYYY-MM-DD-{subject-slug}-handoff.md

Newest first when sorted by name. The handoff skill moves the current
`.claude/handoff.md` here before overwriting it with a new summary.

NOTE: Until Source A and Source B are unified (see memory location registry),
live CC per-project memory lives at `C:/Users/Bean/.claude/projects/{cwd-slug}/memory/`
rather than here. This folder is handoff archive only.
